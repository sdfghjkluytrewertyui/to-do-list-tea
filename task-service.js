// task-service.js

const db = require('./database');

function getAllTasks() {
  // Implement code to fetch all tasks from the database
}

function addTask(task) {
  // Implement code to add a new task to the database
}

function deleteTask(taskId) {
  // Implement code to delete a task from the database
}

module.exports = {
  getAllTasks,
  addTask,
  deleteTask
};
